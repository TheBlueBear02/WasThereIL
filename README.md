# WasThereIL

A mobile-first React web game that shows what percentage of people living in Israel today were physically present in Israel during key historic events.

## Development

```bash
npm install
npm run dev
```

Other scripts: `npm run build`, `npm run preview`, `npm run lint`.
