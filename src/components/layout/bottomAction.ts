/** Shared footer slot so primary CTAs stay pinned to the viewport bottom on every screen. */
export const BOTTOM_ACTION_CLASS =
  'mt-auto shrink-0 border-t border-stone-200 bg-white pt-4 pb-[calc(1.5rem+env(safe-area-inset-bottom))]'
